# Import SOP (RU)

Дата: `<YYYY-MM-DD>`
Проект: `<путь к проекту или название workspace>`

## 1. Входные файлы

- Исходный экспорт:
  - `<source-export-file.xlsx>`
- Рабочий файл для загрузки:
  - `<working-import-file.xlsx>`
- Свежий источник данных:
  - `<catalog-or-api-export.json|csv|xlsx>`
- Таблица сопоставлений / маппинг:
  - `<mapping-file.csv>`

## 2. Обязательные правила

- Идентификатор товара / записи формируем как: `<правило именования>`.
- Цена или расчетное поле считается строго по формуле: `<формула>`.
- Ссылки на документы, медиа или вложения должны вести на: `<целевой домен или хранилище>`.
- Для записей, отсутствующих в свежем источнике, применяем правило статуса: `<архивировать / помечать / пропускать>`.

## 3. Последовательность работ

1. Получить свежий источник данных:
   - `<команда / скрипт / url / путь>`
2. Нормализовать ключевые поля:
   - `<префикс / формат кода / очистка значений>`
3. Обновить целевые поля в рабочем import-файле:
   - Сопоставление по `<ключевое поле>`.
   - Источник значения: `<поле из источника>`.
   - Формула или правило преобразования: `<формула>`.
   - Обновлять поля: `<поле 1>`, `<поле 2>`, `<поле 3>`.
4. Обновить описание / документацию / вложения:
   - Добавить блок: `<название блока>`.
   - Брать ссылки или данные из: `<источник>`.
   - Подменять ссылки или коды по: `<mapping-file>`.
5. Проверить записи вне свежего источника:
   - Область проверки: `<все записи / только с префиксом / только активные>`.
   - Действие при отсутствии: `<маркер / статус / текст / исключение>`.
6. Проверить обязательные поля:
   - Пустых значений в `<поле>` быть не должно.
7. Сформировать финальный файл для загрузки:
   - Основной: `<final-import-file.xlsx>`

## 4. Минимальные проверки перед импортом

- Файл открывается, структура листов не повреждена:
  - `<sheet-1>`
  - `<sheet-2>`
- Нет пустых обязательных значений там, где есть сопоставление в источнике.
- В описаниях и вложениях нет старых или запрещенных ссылок.
- Для отсутствующих записей применено ожидаемое правило статуса.
- Обязательные служебные поля заполнены.

## 5. Импорт в систему

- Загружать файл:
  - `<final-import-file.xlsx>`
- После импорта проверить отчет:
  - `<ошибки = 0>`
  - `<обновлено записей > 0>`

## 6. Что делать при сбоях

- Если файл не открывается:
  - закрыть процесс-редакторы, сделать копию и повторно сохранить исходный формат.
- Если сломана структура листа или набора полей:
  - восстановить из последней рабочей копии и повторить только нужный шаг.
- Если нет сопоставления для документа, категории или записи:
  - оставить без автодобавления, зафиксировать в списке для ручной доработки.

## 7. Контрольные артефакты

- Текущий SOP:
  - `StatusProject/IMPORT-SOP-RU.md`
- Контекст проекта:
  - `TODO-<project>.md`
  - `MEMORY-<project>.md`
  - `PROJECT-RESUME-<project>.md`
  - `STATUS-LOG-<project>.md`
