# CLAUDE.md

- Always respond in Russian.
- For substantial work, read `PROMPT-RU.md` and `START-HERE-RU.md`.
- Enable `StatusProject` when the task is complex and one answer is not enough; create at least `TODO`, `MEMORY`, and `PROJECT-RESUME`.
- If `StatusProject/` exists, read `PLAN`, `TODO`, `MEMORY`, `PROJECT-RESUME`; then optional `STATUS-LOG`, `STATE-HISTORY`.
- Check template updates at most once per 7 days per project; ask before applying and never overwrite local state without approval.
- When deploying, check `.gitignore` using `templates/GITIGNORE.template`.
- Keep root instructions short and point to `StatusProject/`.
